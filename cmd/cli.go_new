package main

import "github.com/spf13/cobra"

var rootCmd *cobra.Command

// init inits
func init() {
	rootCmd = &cobra.Command{Use: "app", Short: "A brief description of your application", Long: `A longer description that spans multiple lines and likely contains examples and usage of your application.`}
}
